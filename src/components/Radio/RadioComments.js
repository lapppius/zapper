import styles from './RadioComments.module.css';

export default function RadioComments() {
    return (
        <section className={styles.radioCommentsContainer}>
            <p>The user comments section</p>
        </section>
    );
}
